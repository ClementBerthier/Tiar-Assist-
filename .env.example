# SMTP credentials for the contact form (server-side only — never NEXT_PUBLIC).
# Gmail SMTP over SSL (smtp.gmail.com:465). Use a Google App Password, not the
# account password.
SMTP_USER=
SMTP_PASS=

# Recipient of contact-form messages. Falls back to SMTP_USER if unset.
MAIL_TO=
